import React from 'react';

const Form = () => (
  <form className="form">
    <h2>ADD NEW BOOK</h2>
    <input type="text" />
    <button type="button">ADD BOOK</button>
  </form>
);

export default Form;
